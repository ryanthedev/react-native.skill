---
description: "Diagnose React Native errors — build failures, red screens, runtime crashes, dependency issues."
argument-hint: "[error text or 'there's an error on screen']"
allowed-tools: ["Bash", "Read", "Grep", "Glob", "Agent"]
---

# /diagnose

## Invoke Skill

```
Skill(react-native-foundations:diagnose)
```
