---
description: "Intelligent environment reset — diagnoses stale caches before cleaning."
argument-hint: "[target — e.g. 'metro', 'pods', 'all']"
allowed-tools: ["Bash", "Read", "Grep"]
---

# /clean

## Invoke Skill

```
Skill(react-native-foundations:clean)
```
